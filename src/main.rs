//! Binary entry point.
//!
//! Keep this thin: wiring only. All logic lives in the library crate so it is
//! unit-testable. This file is excluded from the coverage gate (it is an entry
//! point, per the testing spec).

fn main() {
    println!("service-template — replace this with your service. See README.md.");
}
