# Formatting is mechanical and non-negotiable: `make format` fixes, CI checks.
# Only stable rustfmt options are set here so `cargo fmt` works on the pinned
# stable toolchain without nightly.
edition = "2021"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
